server:
  tomcat:
    #设定tomcat的basedir目录，如果没有指定则使用临时目录
    basedir: /home/app/tomcat_upload_temp
  port: 9013
  ssl:
    key-store: classpath:homyit2023.online.pfx
    #    key-store: /home/ubuntu/JAVA/sll/homyit2023.online.pfx
    key-store-password: 14yvfi39hc

sa-token:
  # token 名称（同时也是 cookie 名称）
  token-name: token
  # token 有效期（单位：秒） 默认30天，-1 代表永久有效
  timeout: 86400
  # token 最低活跃频率（单位：秒），如果 token 超过此时间没有访问系统就会被冻结，默认-1 代表不限制，永不冻结
  active-timeout: -1
  # 是否允许同一账号多地同时登录 （为 true 时允许一起登录, 为 false 时新登录挤掉旧登录）
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个 token （为 true 时所有登录共用一个 token, 为 false 时每次登录新建一个 token）
  is-share: false
  # token 风格（默认可取值：uuid、simple-uuid、random-32、random-64、random-128、tik）
  token-style: uuid
  # 是否输出操作日志
  is-log: true
#  #jwt 密钥
#  jwt-secret-key: ENC(uUcNWBwRxWknG1L08si/9FjZTjka7Y8k5P9MI/RWmSo=)

spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://1.15.115.21:3306/xiaochengxu?allowMultiQueries=true
    username: lzh
    password: 123456
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 3000
  data:
    redis:
      host: 1.15.115.21
      port: 6379
      password: 123456
      database: 2
      connect-timeout: 10s # 连接超时时间
      lettuce:
        pool:
          # 连接池最大连接数
          max-active: 200
          # 连接池最大阻塞等待时间（使用负值表示没有限制）
          max-wait: -1ms
          # 连接池中的最大空闲连接
          max-idle: 10
          # 连接池中的最小空闲连接
          min-idle: 0
  servlet:
    multipart:
      max-file-size: 5GB     #单文件上传大小
      max-request-size: 5GB  #总文件
      location: tempFile
      enabled: true

  mail:
    host: smtp.qq.com
    default-encoding: UTF-8
    username: homyit_123@qq.com
    password: ircxycfzowagchbf
    port: 587
    properties:
      mail:
        debug: true
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

mybatis:
  mapper-locations: classpath:mapper/*.xml
mybatis-plus:
  global-config:
    db-config:
      id-type: auto